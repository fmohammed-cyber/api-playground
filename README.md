# api-playground
This app is an API Explorer that allows Forge developers and admins to test any Atlassian REST API endpoint directly from Jira or Confluence. It acts as a pass-through proxy — users explicitly select and execute endpoints.
